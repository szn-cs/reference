version: "2.10.2"

services:
  kernel-development:
    image: "myuserindocker/kernel-development:init-v2"

    stdin_open: true # docker run -i
    tty: true        # docker run -t
    privileged: true # important to all mounting functionality.

    volumes:
     - ./mount_docker-container:/root/mount_docker-container

    # entrypoint: init.sh
    # Check /root/mount_docker-container; /root/kernel; /root/root_fs;
    command: /bin/bash


