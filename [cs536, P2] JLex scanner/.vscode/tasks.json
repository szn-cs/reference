{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "make",
			"type": "shell",
			"command": "make",
			"options": { "cwd": "${cwd}/src/" }
		},
		{
			"label": "test",
			"type": "shell",
			"command": "make test",
			"options": { "cwd": "${cwd}/src/" },
			"dependsOn": ["build"]
		}
	]
}
