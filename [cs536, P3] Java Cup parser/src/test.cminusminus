// - include code that corresponds to every grammar rule in the file cminusminus.grammar. 
int x;
int y;
bool t;

// Bad inputs - test parser on some bad inputs (IMPORTANT: don't hand those in)






// Example

// input
// if (b == -1) { x = 4+3*5-y; while (c) { y = y*2+x; } } else { x = 0; }

// output 
// if ((b == (-1))) {
//     x = ((4 + (3 * 5)) - y);
//     while (c) {
//         y = ((y * 2) + x);
//     }
// }
// else {
//     x = 0;
// }


// eXAMPLE error
// i.e., expressions like a < b < c are not allowed and should cause a syntax error